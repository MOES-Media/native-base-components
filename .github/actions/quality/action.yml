name: Quality Gate
description: Ensures the continued quality of the code

runs:
  using: composite

  steps:
    - name: 🧹 Lint code
      shell: bash
      run: yarn lint
    - name: 🔨 Build code
      shell: bash
      run: yarn build
    - run: cat /home/runner/work/native-base-components/native-base-components/packages/native-base-atoms/dist/index.js
      shell: bash
    - name: 🧪 Run tests
      shell: bash
      run: yarn test
    - name: 🔬 Analyze code
      shell: bash
      run: echo analysis
    