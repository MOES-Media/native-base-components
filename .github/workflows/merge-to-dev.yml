name: Run After Checks

on:
  workflow_run:
    workflows:
      - "Check PR"
    types:
      - completed

jobs:
  run-after-checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run After Checks
        run: |
          echo "Workflow triggered after checks passed"